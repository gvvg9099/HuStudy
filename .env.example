# Copy to .env and fill values
DATABASE_URL=postgres://postgres:postgres@localhost:5432/hustudy_demo
PORT=3000
# A simple admin token for demo admin endpoints (replace for production)
ADMIN_TOKEN=changeme_admin_token
